1. что такое mdl
2. что такое stack completion в irp пакете

1. отличие крит секции от мьютекса
2. принцип работы системных вызовов

1. точки входа в driver
2. device object
3. отличие крит секции от мьютекса

1.  функция translate message

1. dpc, системный вызов и прерывания

1.  функция translate message

1.  отличия dpc и apc

1. как подключаются dll, loadlibrary, export и import

1. как происходит переход на выполнение другого потока

1. translate message
2. переключение потоков

1. динамическая загрузка dll
2. кто заполняет регистр eax при системном вызове

1. системный вызов

1. точки входа в драйвер


    1) модели виртуальной памяти в пользовательском режиме и режиме ядра
    2) алгоритм системного вызова
    3) кэширование виртуальных страниц

    1) Модели виртуальной памяти в различных режимах
    2) Различия APC и DPC

    1) Отличие APC и DPC
    2) Как создаётся и загружается DLL

    1) Прерывания
    2) IRQL
    3) Lazy IRQL

    1) что такое irp
    2) статический и динамический импорт
    3) отличие dpc и apc

    1) irp
    2) перехват в режиме ядра

    Какие бывают пулы памяти + дополнительные пулы памяти; объяснить

    1) Какие объеткы использует драйвер, пояснить за то, что в объектах
    2) apc/dpc

    Куча+look aside

    1) Методы межпоточной синхронизации
    2) Кэширование страниц в процессоре

    Трансформации матриц

    Точки входа в драйвер

    1) Прерывания
    2) IRQL
    3) Lazy IRQL

    Обработка IRP драйвером + зачем MDL

    Трансп матрицы
    mdl

    Транс матрицы

    1) есть базовый приоритет, а есть динамический. Что за динамический
    2) системный вызов
    3) io_stak_location
